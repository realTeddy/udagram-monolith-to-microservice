apiVersion: v1
kind: Service
metadata:
  name: udagram-backend-feed
  labels:
    app: udagram-backend-feed
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: udagram-backend-feed

 